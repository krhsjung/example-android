package kr.hs.jung.example.ui.theme

import androidx.compose.ui.graphics.Color

// Material Theme 기본 색상
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Background Colors
val Background = Color(0xFFFFFAFA)
val SnsButtonBackground = Color(0xFFFFFAFA)

// Brand Colors
val Brand = Color(0xFFD62600)
val PrimaryButton = Color(0xFFFF5733)

// Text Colors
val TextPrimary = Color(0xFF291C1A)
val TextBlack = Color(0xFF050000)
val TextSecondary = Color(0xFF2E1914).copy(alpha = 0.62f)

// Border Colors
val BorderPrimary = Color(0xFF6E4F4A).copy(alpha = 0.2f)

// Divider Colors
val DividerLine = Color(0xFF6E4F4A).copy(alpha = 0.2f)

// Input Box Colors
val InputBoxBackground = Color(0xFF803624).copy(alpha = 0.09f)
val PlaceholderColor = Color(0xFF2E1914).copy(alpha = 0.4f)

// Checkbox Colors
val CheckboxChecked = Brand
val CheckboxUnchecked = Color.Gray