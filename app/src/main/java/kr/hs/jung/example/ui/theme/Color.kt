package kr.hs.jung.example.ui.theme

import androidx.compose.ui.graphics.Color

// Material Theme 기본 색상
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Background Colors
val Background = Color(0xFFFFFFFF)
val SocialButtonBackground = Color(0xFFFFFFFF)

// Brand Colors
val Brand = Color(0xFF155DFC)

// Text Colors
val TextPrimary = Color(0xFF0A0A0A)
val TextSecondary = Color(0xFF4A5565)
val TextItem = Color(0xFF0A0A0A)

// Border Colors
val BorderPrimary = Color(0xFF000000).copy(alpha = 0.1f)

// Divider Colors
val DividerLine = Color(0xFF000000).copy(alpha = 0.1f)

// Input Box Colors
val InputBoxBackground = Color(0xFFF3F3F5)
val PlaceholderColor = Color(0xFF717182)

// Error Colors
val ErrorColor = Color(0xFFDC3545)

// Link Colors
val LinkTextColor = Brand

// Checkbox Colors
val CheckboxChecked = Brand
val CheckboxUnchecked = Color.Gray

// Button Colors
val ButtonBackground = Color(0xFF030213)
val ButtonText = Color(0xFFFFFFFF)

// Theme Toggle Colors (Light)
val ThemeToggleIcon = Color(0xFF000000)
val ThemeToggleBackground = Color(0xFFFFFFFF)